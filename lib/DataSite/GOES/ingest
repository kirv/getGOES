#!/bin/sh

$self=.

stage=0
tob $self.query-server && {
    stage=1
    tob $self.parse-dcp-message --store && {
        stage=2
        tob $self.parse-good-data --store && {
            stage=3
            }
        }
    }

tob ingest/log.store stage=$stage

